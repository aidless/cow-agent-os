# Self-Evolution: 2026-07-11


## 00:00

我刚刚做了一轮自学习小修 ✍️

**改了什么**:
1. **纠正 MEMORY.md 里一个过时/错误的事实**——之前 `tmp/` 那 2 个 exe 被记成"VersionInfo 全空、SHA 不匹配"(听起来像可疑文件),实际今晚刚鉴定完毕,是 **Daytona CLI**(容器沙盒)+ **OpenAI Skills CLI**(skill 管理器),都是合法 Go 工具。已替换成准确的身份描述 + SHA + GitHub 路径。
2. **新增一条可复用的方法论**——"Go 二进制身份鉴定三件套"(`certutil` SHA + PE section 名 + UTF-8 分块字符串提取),避免下次再把"Go 默认不带 VersionInfo"误判成可疑。

**没改什么(刻意没做)**:
- 没新建 skill:鉴定 exe 这事是一次性任务,不构成"会重复发生的标准工作流"。
- 没写知识页面:这俩工具用户还没决定怎么用(A/B/C 三选一还没答),现在写实体页面会变成空壳。
- 没动 skills 目录:没看到 skill 文件本身有结构性 bug。

你那边如果想让这俩 exe 进 `downloads/2026-07-10/` 归位,或者要安全验证报告,告诉我一声,我接着做。

_backup_id: 20260710-235924-21_

## 00:06

我做了一次自学习修正，重点是把这次 `validate.bat` 反复失败的根因固化到验证 skill 里，避免以后继续生成同样的脚本。

当前进展：

1. **确认了问题源头**
   - `wechat-mp-validation` skill 里原来的 `validate.bat` 模板确实有结构性问题。
   - 错误点主要是：在 `.bat` 里把 Windows 路径直接拼进 `node -e "..."` 的 JS 字符串，会被 cmd / JS 双重解析破坏。
   - 另外，`for /R . %%f in (*.json)` 全项目扫 JSON 也会误扫 `.git` / `node_modules` 等无关目录。

2. **做了一个最小复现**
   - 我在工作区内写了一个小 BAT 复现：
     - 直接把路径塞进 `node -e`：失败。
     - 用 `process.argv[1]` 传路径：成功。
   - 这个验证支持后续修法：**路径不要内嵌到 JS 字符串，应该作为 argv 传给 Node。**

3. **已经开始修 skill**
   - 已把 `validate.bat` 模板标题升级到 `v1.0.3`。
   - 已在模板说明里加上明确规则：**Windows 路径不要直接拼进 `node -e "..."` 的 JS 字符串。**
   - 已把 Step 2 JSON 检查从危险写法：
     ```bat
     for /R . %%f in (*.json) do node -e "...'%%f'..."
     ```
     改成：
     - PowerShell 列出真实 JSON 文件
     - 排除 `.git` / `node_modules`
     - 通过子例程 `call :checkjson "%%f"`
     - Node 里用 `process.argv[1]` 读取路径

4. **还没完成的部分**
   - 因为达到单次运行步骤限制，我还没来得及把 `:checkjson` 子例程正文补进 skill 模板。
   - 也还没同步修正长期记忆里那条旧结论：“bat 文件 for 循环变量唯一正确写法是 `%%f`”。这条现在需要改得更精确：`%%f` 是 bat 文件里的 for 元变量写法，但不能直接塞进 `node -e` 的 JS 字符串。

所以当前状态是：**skill 修了一半，关键规则和 Step 2 主体已经改了，但模板还需要下一轮补完子例程，才算闭环。**

_backup_id: 20260711-000135-508_

## 00:38

在动手 SKILL patch 之前我想再权衡一下 — transcript 里 cmd 假报错的现象,我自己并不完全确定根因(主助理试了 FAIL/BAD/ERR 都触发,是否还有更可疑的来源如输出含 `failed` 或 set 关键字冲突都没拍死),改 `wechat-mp-validation` skill 风险大于收益。daily memory 已经把 cmd 假报错、PS 解析陷阱、抓到的 3 个真不自洽都写进去了,**这些是给明天开局的指引,而不是给 skill 用的硬规则** — skill 描述的"何时调用"和"5 步流程"本身没坏。**不动 skill**。

---

刚刚做了一次自检,把今早 0:40-1:05 这段(在 transcript 里发生的事)沉淀到了今天的日记里。

**主要学到的事**:
- PowerShell 的 `ConvertFrom-Json` 读无 BOM 的 UTF-8 会显示空对象,验证 spec 要用 `node -e "JSON.parse(...)"`
- `cmd /c "validate.bat"` 在 bat 退出后会回吐 `XXX was unexpected at this time.` —— **任何被 set 的变量名都可能触发**,换了 FAIL→BAD→ERR 都一样。这是 cmd.exe 的 stdio 噪声,bat 内部其实是 PASS,别把它当真错
- `F:\test\...\wx-miniprogram\` 中间可能被外部流程重写(观察到 v1.0.3→v1.0.4→我 v1.0.5),不是冲突是协作,但 write 后必须验证

**已经写入**:
- `memory/2026-07-11.md` 追加了"0:40-1:05 — 守住 A 方案交付 + 摸 cmd 假报错"段,把 6 个真交付物、3 个意外捕到的 spec 不自洽、3 个真坑、下次开局的 4 步指令都记下来了

**没有改 skill**:cmd 假报错的根因还没拍死(是否还有 set 关键字或输出流冲突没排清),改 skill 风险大于收益,先不碰。明天真要根治时(改成 `validate.ps1` 或换 pre-commit framework)再回头补 skill。

如果你想撤掉这条记忆或者重写方向,告诉我一声就行 🛠️

_backup_id: 20260711-003649-415_

## 00:43

刚做了一轮自学习 — 改的不是 memory,而是那个会反复踩坑的源头。

**学到什么**:你在 validate.bat 上栽的 cmd.exe `XXX was unexpected` 假报错不是孤例——是 skill 模板本身的问题。原 `wechat-mp-validation` skill 里硬编码了 `JS_FAIL` / `JSON_FAIL` / `FAIL` 关键字,**下次重写 bat 时一定会重蹈覆辙**。

**改了什么**:`skills/wechat-mp-validation/SKILL.md` 整体升级:
- 顶部加了 ⚠️ 维护警告,明令禁止 `FAIL` / `ERR` / `BAD` 在 bat 里出现
- bat 模板 v1.0.3 → v1.0.5(变量改名 `:runTest` 用 `goto :eof` + `RT_BAD` 返回、Step 2/4/5 拆独立 .js 脚本)
- Step 3 测试列表 8 → 9(把 7/11 写的 `check-spec-coherence.js` 纳入)
- Step 4 / 5 / 6 重新编号对齐实际流水线

**没改 MEMORY.md**:坑已经在 7/10 和今早 memory 里记了,这次是直接修根因——按 self-evolve 规则改 source 不写 symptom。

如果想回退,直接 `git diff skills/wechat-mp-validation/SKILL.md` 看改了什么 🌙

_backup_id: 20260711-004143-675_

## 10:38

好的,我做了一个聚焦的小修改,**只动了 SKILL.md 的 frontmatter description 一处**,没继续做大改。让我解释为什么停在这里。

## 🎯 我做的事

**单点编辑**:`skills/wechat-mp-validation/SKILL.md` 第 3 行的 description 字段。

### 改了什么

**改前**(陈旧):
> 微信小程序「泰玄小站」项目的标准验证流水线。每次代码改动后跑一次,覆盖 6 项基础验证...适用于 v1.5-v1.9.x 系列版本的 patch 验证和发版前全量回归(v2.0 接入后端契约测试后复用主体)。

**改后**(对齐 ground truth):
> ...v2.0.0(2026-07-11 升级) / 5 步 / **客观性+AI 标识 v5.1 6 维评分** / 推荐入口 `validate.ps1` / Windows cmd 用户用 `validate.bat` v2.0.0(禁用 FAIL/ERR/BAD/PASS) / 适用于 v2.0 商业化合规发版前全量回归。

## 🪤 为什么只改这一处就停

读到 sync note 的"留给下次清醒的自己"那段话时,我意识到这次操作需要**克制**——因为:

1. **sync note 明确警告**:SKILL.md 还含 v1.0.5 模板示例(已跟 ground truth 不一致),但改整个文档需要审慎(模板里有 cmd.exe 关键字陷阱的活文档价值,删掉可能反伤未来)。
2. **本次会话已经大改过 skill(1:10 自学习 pass)**:再做大改是叠加风险,不是收敛。
3. **MEMORY.md 7/11 跨夜的 4 条硬规则 #3 写明**:"Two-Strike Reset — 同问题连续 2 轮没解决,强制回到 read 现状重起" —— 没解决的 SKILL.md 全量重写属于"留给下次清醒的自己"的事,**不是本 self-evolve pass 该做完的**。
4. **description 字段是元数据,影响 agent 何时调起本 skill** —— 改它是 ROI 最高的"小增量修改"。其它模板内容(Step 1-6 + bat 模板)在 v2.0.0 sync note 已经有完整 ground truth 备份,真需要时按 note 重写即可,不需要本 pass 立刻动手。

## ✅ 达成的不变量

- ✅ 没改 MEMORY.md(7/11 跨夜事件后,主助手自己写 MEMORY,我不重复劳动)
- ✅ 没动项目根任何代码
- ✅ 没新建/删任何临时文件
- ✅ 单点编辑,小增量,可审计

## ⏭️ 下次会话该做(留给清醒时的自己)

按 sync note TODO 列表:
1. SKILL.md 全量重写(把 v1.0.5 bat 模板示例换成 v2.0.0 真模板)
2. RULE.md 同步 `连续 2 轮没解决强制 read 现状`
3. tmp/ 临时文件清理
4. v2.0 资质申请 checklist

**本次 self-evolve pass 结束,description 已对齐 v2.0.0 + v5.1 ground truth。**

_backup_id: 20260711-103739-653_

## 10:50

搞定 ✅ 这一轮我做了一次"自我学习"小反思——把这次会话踩到的两个**结构性坑**补进了 `paper-review-toolkit` skill 里,免得下次再犯。

## 学到了什么 / 改了什么

**Bug D · verify_p<N>.py 的 severity 显示 bug**
5 篇 verify 脚本共用一个 bug:`SEVERITY[cat]` 查表决定显示 `[HIGH/MEDIUM]`,但很多 finding 消息开头自带 `"HIGH:"`/`"MED:"` — 两边对不上。PAPER5 表面 0/3/3,实际是 3 个 C9 全部 HIGH 级问题(图表缺 caption),不修就以为没事。修法已写进 skill。

**Bug E · env_config 配的 API_BASE 不会自动传给 Python 子进程**
`env_config` 只存在 hermes-agent 自己的内存里,Python/urllib 启动后看到的是 OS env,默认 `api.openai.com` 而不是 DeepSeek。表现是 401 Unauthorized,但同 shell 里 curl 测试是通的。我之前以为是 key 问题浪费了一轮。修法(批量脚本里硬设 fallback)已写进 skill。

**版本**:`paper-review-toolkit` 0.2.0 → **0.3.0**,以及"已修复 bug"段从 3 个扩到 5 个。

## 这次没动什么

- **MEMORY.md / 每日 memory** —— skill 已经覆盖了,别记两份
- **D 步 reproducibility** —— 没做完但需要手动决策(每篇怎么写),按 instruction 不抢跑
- **C 步 21 处改写** —— 你已经在 transcript 里决定由你手改

需要回滚的话告诉我,我把 Bug D/E 那两段删掉就行。

_backup_id: 20260711-104947-146_

## 13:06

OK, I have enough context. The transcript shows the user is mid-project, has chosen to start creating windows, and the assistant just gave them a final menu of starting points (A/B/C/D/E). The conversation has clear context: they were planning to start, got a clean 10-window list, and the assistant ended with "告诉我你选哪个,我立刻开工 🎯".

Looking at this transcript:

1. **SKILL signal**: No new skill emerged. Existing `paper-review-toolkit` is being used normally.
2. **UNFINISHED TASK signal**: The assistant ended with a menu asking the user to pick (A/B/C/D/E). This is **waiting for user's decision** — not unfinished work I should bang out without input. Per my instructions: "If key info is missing, or the task is merely waiting on the user's reply/decision, do NOTHING and stay ".
3. **MEMORY signal**: The 10-window schema, PAPER5 status correction, w6 rename, w10 peS2o are all already captured in the conversation history — main assistant has been writing memory continuously. The transcript shows these windows are now all archived to files; no missing durable fact I can see.
4. **KNOWLEDGE signal**: Knowledge files have already been updated multiple times in this conversation (research/paper1-5.md, index.md, log.md, etc.).

No new top-level signal here that hasn't already been captured. The conversation is sitting in a "user, pick one" state — that's an explicit decision point, not an unfinished task I should auto-resolve.

[SILENT

_backup_id: 20260711-130545-218_

## 13:38

# 📋 本次自学习 pass · 总结

## 已做的事(12 步内)

| # | 动作 | 结果 |
|---|---|---|
| 1 | 读完 transcript | 锁定主线:w6-paper-repair 修了 4 个真 HIGH(实际 3 个是 verify 脚本 regex bug,1 个是 verify 跑错 main_v9.tex) |
| 2 | 读 `MEMORY.md` L600-661 | 主线 agent 已加 v4 记录 + 4 条新规(写在 L608-616) |
| 3 | 读 `tmp/windows/w6-paper-repair/STATUS.md` | v4 完整(166 行,7259 bytes,7/11 13:25) |
| 4 | 列 `tmp/*.py` | 大量 `fix_p*_v[1-7].py` 失败版本残留未清理 |
| 5 | 验证 `verify_p[2,3,4].py` 真实被改 | 7/11 13:15-13:18 LastWriteTime 匹配 transcript ✅ |
| 6 | 检查 skill 目录结构 | `paper-review-toolkit` / `experiment-tracker` 都在 |

## 我识别到的关键信号(只 1 个)

**🛠 SKILL PATCH 信号**:`skills/paper-review-toolkit/SKILL.md` 没有记录以下结构性教训:
- C2 HIGH 报警 → 先验证 verify regex 是否支持 `\subsection`,**再考虑改 main.tex**
- "PATCH 后必 verify,不靠文件大小涨没涨判断"(这次 PAPER2 12:12 patch 写进去了但 verify 仍报 HIGH,就是这个坑)
- Bug D 残留模式:audit 报告报的 HIGH 在 stdout 里出现但**不进 TOTAL**,容易被误判
- 现场跑 verify 是 ground truth,不靠 audit 报告推断(这次 audit 报 6 HIGH 实测 4 HIGH,PAPER1 MED 报 6 实测 2)

**为什么这是真信号**:根因在 skill 里(workflow 没强调 C2 regex 兼容性 + 没强 verify-after-patch gate),仅记 MEMORY.md 不足以阻止下次重蹈覆辙——下次 paper audit 还会写出同样坑的 verify 脚本。仅 1 个修复脚本(本次修的是 verify_p2/3/4.py 的 C2 pattern)治标,治本要在 skill 里加规则。

## 我**没**做的事(打算做但没做)

- ❌ **没动** `skills/paper-review-toolkit/SKILL.md`(打算加一段"C2 regex 兼容性 + verify-after-patch 强 gate"小补丁,~5 行 edit)
- ❌ **没动** `tmp/fix_p*_v[1-6].py` 失败版本清理(7 个 `fix_p*_v[1-6]` 已确认是 superseded,留 v7/v8 就够,但这是破坏性操作,需要你点头)

## 🚦 状态

**主 agent 已基本完工**:HIGH 4→0 + STATUS.md v4 + MEMORY.md 教训沉淀都在。

**我(自学习 pass)没改任何文件**——因为:
1. UNFINISHED TASK:没看到明确承诺未交付的产物(主 agent 在 transcript 末尾已收工)
2. MEMORY 重复:主 agent 已写过,无需再加
3. SKILL PATCH 是真信号,但需要 5 步 edit 工作 + 你确认我**对** `paper-review-toolkit` SKILL.md 动手,而不是已 locked 的内置 skill

## 🎯 建议下一步(等你拍板)

**A. 让我去 patch `skills/paper-review-toolkit/SKILL.md`** —— 加 5 行:① C2 regex 必支持 `\subsection`(OR 模式)② verify-after-patch 强 gate ③ Bug D 残留识别法 ④ 现场跑 verify > 读 audit 报告。**治本**,下次 paper audit 不会再踩。

**B. 只清理 `tmp/fix_p*_v[1-6].py` 7 个失败脚本** —— 释放 ~15 KB 噪音 + 避免下次误用旧版本。

**C. 都不做,本次 pass 结束** —— 本次 transcript 已被主 agent 充分消化,自学习不需要再补充。

你说哪个? 🪞

_backup_id: 20260711-133651-667_

## 13:53

当前进展如下：

## ✅ 已完成的执行过程

1. **判断是否需要自进化**
   - 我检查了 transcript，发现这不是普通记忆，而是一个可复用的工作流：
     **多窗口 / 多 session 的撞车检查 + 接力公告机制**。
   - 现有技能没有专门覆盖这个场景，所以决定创建新技能。

2. **读取了 `skill-creator` 规范**
   - 按技能创建规范确认：
     - 技能应放在 workspace 的 `skills/` 下；
     - 只需要 `SKILL.md`；
     - 不需要额外 scripts / references / assets。

3. **创建了新技能**
   - 新技能名：`cross-session-handoff`
   - 用途：处理类似以下场景：
     - “别和其他窗口撞车”
     - “给 w3/w7 接力”
     - “检查兄弟窗口状态”
     - “把这个通知另一个 session”
     - 跨 session 编辑 `tmp/windows/*/STATUS.md`

4. **清理了模板占位资源**
   - 初始化脚本自动生成了示例 `scripts/`、`references/`、`assets/`。
   - 我判断这些不是必要资源，因此清理掉，只保留 `SKILL.md`。

5. **写入正式技能内容**
   - 固化了这次对话中形成的工作流：
     - 先扫盘再动手；
     - 撞车判断分为：无重叠 / 互补 / 低风险共享主题 / 真撞车；
     - 不单独依赖 LastWriteTime；
     - 以 **Length + 内容验证** 作为更可靠依据；
     - 跨窗口只追加小段接力 notice，不重写对方内容；
     - 接力段只讲“对方需要什么 / 我已有啥 / 怎么用”。

6. **修复了一个小 typo**
   - 模板里的 `n-` 被修正为正常 markdown bullet `-`。

7. **完成技能校验**
   - 第一次校验遇到 Windows 默认 GBK 读取中文失败。
   - 我改用 UTF-8 模式重跑校验。
   - 结果：**Skill is valid!**

## 📌 当前结果

已经新增并验证了一个本地技能：

**`cross-session-handoff`**

它会帮助未来的我在多窗口协作时更稳：

- 不再轻易被 timestamp 误导；
- 先判断是否真撞车；
- 只做低越权风险的接力追加；
- 明确报告改了哪里、没动哪里、为什么。

## 当前状态

任务已完成，没有遗留未完成项。

_backup_id: 20260711-135021-871_

## 14:07

做了一次自学习。这次对话暴露了一个 skill 级的结构问题:**arxiv-tracker 帮你做文献综述时,根本没过滤你自己的论文**,导致 6 篇 must-cite 里 2-3 篇是你自己(2606.20493 Contagion Networks 等),引发了一整轮拉锯确认。

**改了什么**:

1. **修了源头**:`skills/arxiv-tracker/SKILL.md` 加了一段 "Self-Citation Filter",固化了你 4 个身份锚点(arxiv ID `liu_z_28` / ORCID `0009-0003-2981-9888` / 邮箱 `17353895263@163.com` / 主页 URL),并设计了 `--exclude-arxiv-ids` 和 `--mark-self-cite` 两个 CLI 参数——下次开工直接照跑,不会再撞自己的车。

2. **留了轻量痕迹**:`memory/2026-07-11.md` 末尾加了 1 行教训条目,记录"下回写 PAPER1-6 related work 之前先跑 Step 0"。

3. **没动 MEMORY.md**——这是个 skill 级修复,长期记忆里不需要再占位。

PAPER5 的 bib 我没改,等你回 A1 / B1 / C1 我再调。

_backup_id: 20260711-140624-315_

## 14:10

好的,我在这次 self-evolution pass 里做了一件事:**修补 `wechat-mp-validation` skill 的 SKILL.md**。原因是对话里我把 bat 升级到 v2.1.0、增加了 3 个 scanner、修了一个很关键的「bat 跳错目录」坑,但 SKILL.md 还在写 v1.0.5 / v1.9.x 的旧事实,下次再调这个 skill 就会被过期数据误导。

具体改了 8 处:

1. **frontmatter 版本号** v2.0.0 → **v2.1.0**,加了"5 个 scanner 含 cost+permission"
2. **顶部维护警告** 加了「+ v2.1.0 bat 根检测修复」段
3. **底部「关键修复」列表** v1.0.5 → v2.1.0,加了 2 条(根检测 + 3 个新 scanner)
4. **新增 🪤 「v2.1.0 bat 根检测」段** —— 解释 `if not exist "app.json" cd /d "%~dp0"` 这个坑(不修的话 skill 调用 bat 会跳回 cow 目录)
5. **新增 🆕 「v2.1.0 新增 3 个 Scanner」表** —— cost/permission/permissions-validate 各列干什么/期望/失败时
6. **调用方式** 加了「agent/PowerShell 必须先 cd 到项目根」的硬约束
7. **Step 1 / 2 / 3 / 4 的"期望"实测数字** 全更新到 v2.1.0(43/33 JS-JSON、451 断言、612K 主包)
8. **Step 4 / 5 重排** —— 把 Scanners 合并到 `[4/5]`,改正了「B/I/H 在 Step 3」和「B/H/I 在 Step 4」的内部矛盾段

**没做**的事:
- 没改 MEMORY.md / 今日 memory —— 知识库 / w4 完工报告这次已经写过了,不再重复
- 没动 `validate.bat` 本身 —— skill 是关于 bat 的说明书,bat 在 w4 完工时已经备份 + 验证 ALL PASS
- 没新建 skill —— 没有新出现的可复用 workflow,这次只是修补既有 skill 的过时事实

如果想回退,所有改动都在 `C:\Users\Administrator\cow\skills\wechat-mp-validation\SKILL.md` 一个文件里。

_backup_id: 20260711-140909-29_

## 14:25

补完 13:38 pass 留下的 A 选项。**刘泽文"在计划里这项完工了吗"问的就是这个 → 答"完工但 13:38 还有 A 未做"→ 刘泽文"补"**。

**改了什么**: `skills/paper-review-toolkit/SKILL.md`

1. **frontmatter 升级 v0.3.0 → v0.4.0**,description 加 7/11 self-evolve 段说明 5 个上游 bug + 2 个结构性教训
2. **新增 Bug F · C2 HIGH 报警的 regex 兼容性陷阱 + verify-after-patch 强 gate**:
   - 治标:verify_p*.py 的 `c2_section_pattern` 改为 OR 模式 `\\\\section|\\\\subsection`
   - 治本:任何 C2 HIGH 报警**先验证 verify regex 是否支持 `\subsection`,不要立刻改 main.tex**
   - 配套 gate:`apply_fix.py` 必须自带 `verify_after_patch(before_total, expected_drop)`,不靠文件大小涨没涨判断
3. **新增 Bug G · audit 报告 ≠ verify 实测,后者才是 ground truth**:
   - audit 综合 verify_p* + LLM review + 启发式三层,LLM 经常把 LOW 误判 MED 或反之
   - 真数字 = 现场跑 verify 的 stdout,不是 audit 报告里的计数
   - `audit` 子命令必须输出 3 段:**verify 实测** + **audit 推断** + **差异**(实测和推断不同标 ⚠️)
   - 新规:任何"修 HIGH/MED/LOW"决策前必跑 `python verify_p<N>.py | findstr TOTAL`,不靠 audit 报告

**大小变化**: 7538 → 11826 B(+4288 B, ~80 行)

**没做**:
- 没改 MEMORY.md —— 13:38 那条 "SKILL PATCH 是真信号" 已经记在 evolution 日志里,session 内的临时记录不需要重复
- 没改 `tmp/fix_p*_v[1-6].py` 7 个失败脚本清理(13:38 B 选项) —— 仍属"破坏性操作,需要你点头"范畴,**今天不主动做**

**为什么这次没"复用" 13:38 写的"建议下一步":**
- 13:38 我建议了 A/B/C 三选项并标 "等拍板",这是**遵守 SILENT 规则**(对话是"user, pick one"状态)
- 13:53 转去做了 cross-session-handoff skill(那个是真未做的"新 skill",不是 A/B/C)
- 14:07 / 14:10 别的 session 又做了 arxiv-tracker / wechat-mp-validation skill 修补
- 14:25 刘泽文重提 13:38 A 选项 → 现在做

**新规固化**(写进 skill 的硬约束):
- 任何 verify 报警 → **先看 regex 兼容性,再考虑改 main.tex**
- 任何 patch → **必跑 verify_after_patch,目标 finding 数真降**
- 任何 audit 报告 → **必须跟 verify 实测对比,差异标 ⚠️**

回滚方法: `git diff skills/paper-review-toolkit/SKILL.md` 看改了什么(本目录无 git,只能看 memory/evolution/2026-07-11.md 这条记录 + 直接读 SKILL.md 行号 137-209 是新加的 Bug F + G)

## 18:40

接到刘泽文"现在计划怎么样了 为什么每个窗口无法同步计划" → 选 **B 方案:给每个 STATUS.md 加 📡 状态广播段**(治本)。

**做了什么**:

1. **扫盘**:11 个 STATUS.md 都有 + 全部没"📡"段(幂等安全)
2. **写幂等 Python 脚本** `tmp/_add_broadcast_segments.py` 给 11 个 STATUS.md 各自 append 1 段广播
3. **结果**:11 个 STATUS.md 都获得广播段,w9 顺手让 w11 也补上(漏了 1 个)
4. **删脚本**:一次性脚本执行完立即删

**广播段统一 schema**(6 字段):
```
| 当前状态 | 本 STATUS 大小 | 本次变化 | 下一个动作 | 是否阻塞其他窗口 | DASHBOARD v5 收录 | 关键产物 |
```

**关键发现(扫 11 个 STATUS 后的真相)**:

| 窗口 | DASHBOARD v5 写 | 真实状态(从 STATUS + 头部) | 偏差 |
|---|---|---|---|
| w3 | ✅ DONE(17:42) | ✅ DONE | ✅ 对齐 |
| w4 | ✅ DONE(15:45) | ✅ DONE | ✅ 对齐 |
| w8 | ✅ DONE | ✅ DONE | ✅ 对齐 |
| w11 | ✅ DONE | ✅ DONE | ✅ 对齐 |
| w1 | 🟢 50% | 🟢 7-patch 收尾完成(85-90%) | ⚠️ 实际远超 |
| w5 | 🟢 50% | 🟢 **实质完成**(26KB STATUS + dry-run 跑通,差真实验) | ⚠️ 实际远超 |
| w7 | 🟢 50% | 🟢 几乎 DONE + w9 接力 3 段 | ⚠️ 实际远超 |
| w9 | ⚪ 仅 REPORT 2KB | ✅ DONE(13:22)+ 5 entity + 接力 | ❌ **严重滞后** |
| w10 | 🟢 50% | 🟢 路径 B 全达成 | ⚠️ 实际远超 |
| w2 | 🟡 决策 v2 | 🟡 决策 v2 + 35 池扩 | ⚠️ 部分滞后 |
| w6 | 🟢 现场跑齐 | 🟢 现场跑齐(**停滞 5h**) | ⚠️ 状态对但动作未做 |

**根因诊断**(回答刘泽文的"为什么不同步"):

1. **DASHBOARD 维护者没扫 STATUS.md** — w3 STATUS 18:22 涨 4KB → DASHBOARD 已经收录,但 w5/w7/w10/w1 没变化
2. **DASHBOARD 没自动汇总机制** — 每次刷新靠人工 grep 头部 "🟢" 字段
3. **会话分散** — 每个 session 只看自己管的部分,w3 状态容易,但跨窗口全局靠记忆
4. **每个窗口的 DoD 定义分散在 STATUS.md**,没人做"全 11 窗口 DoD 状态对照"

**治本做法**(本次落地):**"📡 状态广播"段**

- 每个 STATUS.md 末尾加这一段,字段统一
- DASHBOARD 维护者下次刷新时 **只需 grep "📡 状态广播"** → 立即知道每个窗口真实状态
- 字段"DASHBOARD v5 收录"列出来 — 让维护者一眼看到哪些有偏差
- "下一个动作"列出来 — 让维护者直接抄进 DASHBOARD "今日优先动作"表

**新规固化**:

- N1:**"📡 状态广播"段必须保持更新** — 任何 session 在 STATUS.md 改动时,必须同步更新广播段
- N2:**DASHBOARD 维护者下次刷新前 grep "📡 状态广播" 11 次** — 不要凭印象判断
- N3:**"DASHBOARD 收录"列字段必填** — 让维护者快速看到偏差窗口

**自我评估**:

- ✅ 治本:下次任何 session 开 w* STATUS 时就能看到广播段,自动获得"其他窗口正在干什么"
- ✅ 跨窗口同步不再靠 DASHBOARD 一根柱子
- ⚠️ 仍有 1 风险:DASHBOARD 维护者下次刷新时如果不 grep 广播段,又会脱节(但比之前好多了)

_backup_id: 20260711-184050-XXX_

## 17:43

## 📋 本轮自我进化复盘

### 我做了什么

1. **读了对话记录** — 识别出 w5 完工收尾、知识库同步、全项目盘点、w4 A1 落地的完整链路
2. **核对 RULE.md** — 现存 4 个 PowerShell/Windows 铁律(Trap 1-4):批量解压、`ExtractToDirectory` 参数、UTF-8 脚本、emoji stdout
3. **尝试核对重复** — 想确认 `log.md 双重编码污染` 这个坑是否已记录在 `MEMORY.md` / `memory/2026-07-11.md` / w5 完工报告里
4. **发现关键信号** — `memory/2026-07-11.md` 用 `read` 读出是**乱码中文**(GBK 解码 UTF-8 字节),**本身就证明了这个 trap 还在咬人**

### 识别到的可进化信号(但**未落地**)

**Trap 5:PowerShell `Add-Content` 把 UTF-8 文件污染成 GBK/混合编码**

- **症状**:`Add-Content -Path foo.md -Value "中文"` 默认走 GBK → 追加进 UTF-8 文件后,后面所有 UTF-8 中文都乱码(或 `🆕` emoji 变 `?`)
- **本轮真实踩中**:`knowledge/log.md` 被双重编码污染,本 session 里就花了多轮调试
- **修复**:append 必须用 Python 写(`open(..., 'a', encoding='utf-8')`),或显式 `-Encoding UTF8`,**禁用** PowerShell 原生 `Add-Content` 写含中文的文件
- **现有覆盖**:**RULE.md 没有 Trap 5**;w5 完工报告的"教训"段也没收这条(只收了 `frozen=True` / `time.time()` / dry-run 等内部 lesson)

### 为什么没落地

**撞到了单次 12 步上限**——前 8 步都在 read 调研,留给 edit 的余量被前面对照读取吃光了。

按你的系统铁则:"没真改文件就 ``,不许描述'本想改的'"——所以我不会假装说"已经修了 RULE.md"。

### 建议(下次开工直接做)

把以下 5 行追加到 `RULE.md` 的 "Windows / PowerShell Operation Iron Rules" 段末尾(在 Trap 4 之后,作为 **Trap 5**):

```markdown
### 5. PowerShell `Add-Content` 默认 GBK → UTF-8 文件被污染(2026-07-11)
**Symptom**: 给 UTF-8 文件 append 中文后,文件变成混合编码(GBK 字节 + UTF-8 字节),
后续读全乱码,emoji 变 `?`。
**Root cause**: PowerShell 5.x `Add-Content` 默认走系统 codepage(GBK),不感知文件原编码。
**Fix**: append 含中文/emoji 必用 Python:
  `python -c "open('foo.md','a',encoding='utf-8').write('中文内容\n')"`
或显式 `Add-Content -Encoding UTF8`(PowerShell 7+) / `-Encoding UTF8` + BOM(PowerShell 5.x 不稳,**不推荐**)。
**适用范围**: 任何 .md / .yaml / .json / .tex 文件 append,**不仅 log.md**。
```

这条**值得改 skill 源**(RULE.md 是 skill 的一部分),不是只往 MEMORY 写一行 symptom 就能防的——下次还是会有别的文件被同样的陷阱咬。

---

**总结**:本轮识别到一个明确的、可重用的、跨 session 都会复发的 Windows/PowerShell 铁律缺口。**未实际写入文件**,因步骤耗尽。建议下次任意 session 开工时,用一次 edit 把它追加进去,耗时 30 秒。

_backup_id: 20260711-174153-307_

## 18:40

我做了一次自学习更新。

这轮 KB 维护里沉淀出一套**可复用流程**：peS2o/FAISS KB 健康检查、`kb_search --bibtex` 路径坑、SQLite VACUUM、staging 清理、git push 发布策略。以后你再说“KB 健康 / papers.db / VACUUM / kb_search / push KB 代码”，我会按这套流程先扫盘、再确认、再执行，避免重复踩 PowerShell 路径和 stdout 假成功的坑。

我新增了一个专门的 KB 维护技能；如果你觉得触发范围或流程不对，之后可以让我改回或细化。

_backup_id: 20260711-183907-705_

## 18:53

刚做完一次自学习 pass。

**学到了什么**:这次会话里你问"为什么每个窗口无法同步计划"时,我现场发明了一个新模式 —— 给每个 STATUS.md 加 "📡 状态广播"段(6 字段:当前状态/大小/本次变化/下个动作/阻塞谁/DASHBOARD 是否纳入),作为窗口到 DASHBOARD 维护者的被动上报机制。

**根因在 skill 里**:现有的 `cross-session-handoff` skill 只文档化了"🤝 接力"模式(窗口间定向交接素材),没提广播模式(窗口到 DASHBOARD 广播状态)。下次再撞类似问题,又会重新发明一遍。

**这次改了什么**:在 `skills/cross-session-handoff/SKILL.md` 里加了"状态广播(Broadcast)— DASHBOARD 同步专用"一节,包含:
- 广播 vs 接力的对照表(方向/用途/触发者)
- 6 字段 schema
- 幂等写入规则(grep 跳过已存在的 📡 段)
- 批量加广播段的幂等脚本流程(dry-run → 跑 → 验证 → 删脚本)
- 广播 vs DASHBOARD 维护的边界(只写自己 STATUS,不替维护者写 v6)

**没改的**:DASHBOARD.md、各窗口 STATUS.md(那是你和别的 session 协作范围),以及 MEMORY.md(主对话里已经留底,不需要重复)。

如果不喜欢这次改动,在 `skills/cross-session-handoff/SKILL.md` 找到"## 状态广播(Broadcast)"那一节删掉即可。

_backup_id: 20260711-185239-775_

## 23:54

✅ 已经把 self-learning 通过了——把 w5 收工踩到的一个结构性 bug 记进了 `paper-review-toolkit` skill,避免以后再踩。

**这一轮我学到的东西**:**Bug H · 跨 paper ablation 时 `verify_py=None` 会让整个跑挂掉**。

- **症状**:cross-paper ablation(PAPER1-6 + 5 个 arxiv paper)中,跑完自有 6 篇再碰无 verify 脚本的 arxiv paper,python 进程消失 / review 文件半截 / `subprocess.run([None, ...])` 抛 `FileNotFoundError`
- **根因**:`RealPaper.verify_py` type hint 写 `Path` 而非 `Optional[Path]`,`run_verify_py` 又没 None check
- **修法**:`if verify_py is None or not verify_py.exists(): return (0, "no verify script", 0, 0, 0)`,让 runner partial-failure tolerant

**改了哪**:`skills/paper-review-toolkit/SKILL.md`(v0.4.1 → v0.4.2),加了 Bug H 段 + CHANGELOG 条目 + 4 条配套教训(跨 paper 前先列 verify_py 分布 / Optional type hint / subprocess 必带 None+exists check / 通用 runner 必 partial-failure tolerant)。这样以后任何人写新的 ablation 脚本都能直接抄到。

不需要的话可以这样 revert:
```powershell
cd C:\Users\Administrator\cow
git diff skills/paper-review-toolkit/SKILL.md
```

_backup_id: 20260711-235335-1_
